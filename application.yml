server:
port: ${SERVER_PORT:8089}
logging:
level: 
    org.springframework.security: DEBUG
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/meal_planning_system?allowPublicKeyRetrieval=true&useSSL=false
        username: admin
        password: pokemon,J.2
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate.ddl-auto: update